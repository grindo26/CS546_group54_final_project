<title>{{title}}</title>
<h1>User Profile</h1>
<p>Name: {{user.name}}</p>
<p>Username: {{user.username}}</p>
<p>Email: {{user.email}}</p>
<p>Age: {{user.age}}</p>

     <!-- Tabbed interface -->
        <div class="tabbed-content">
            <!-- Tab list -->
            <ul class="tab-list">
                <li class="active" data-tab="comments">Comments</li>
                <li data-tab="reviews">Reviews</li>
                <li data-tab="posts">Posts</li>
            </ul>

            <!-- Tab content -->
            <div class="tab-content" id="comments">
                <!-- Display the user's comments here -->
                 <p>Comments added by you:</p>
                    {{#each displayComments}}
                   <p>{{this.commentorId}}</p>   
                {{/each}} 
            </div>
            <div class="tab-content" id="reviews">
                <!-- Display the user's reviews here -->
                 <p>Reviews added by you:</p>
                  {{#each displayReviews}}
                   <p>{{this.review}}</p>   
                {{/each}} 
            </div>
          <div class="tab-content" id="posts">
              <p>Attractions added by you:</p>
                {{#each displayAttractions}}
                   <p>{{this.name}}</p>   
                {{/each}} 
            </div>
        </div>

          <style>
            /* Styles for the tabbed interface */
            .tabbed-content {
                display: flex;
                flex-direction: column;
            }

            .tabbed-content>div {
                display: none;
                /* Hide all tab content by default */
            }

            .tabbed-content>div:first-child {
                display: block;
                /* Show the first tab content by default */
            }

            .tab-list {
                display: flex;
                list-style: none;
                margin: 0;
                padding: 0;
            }

            .tab-list li {
                flex: 1;
                text-align: center;
                border: 1px solid #ccc;
                border-bottom: 0;
                background-color: #f1f1f1;
                cursor: pointer;
            }

            .tab-list li.active {
                background-color: #ddd;
            }
        </style>

        <script>
            // JavaScript to handle tab switching
            const tabList = document.querySelector('.tab-list');
            const tabContent = document.querySelectorAll('.tab-content');

            tabList.addEventListener('click', (event) => {
                // Get the clicked tab
                const clickedTab = event.target;

                // Make sure the clicked element is a tab
                    if (clickedTab.tagName === 'LI') {
                    // Remove the active class from all tabs
                    tabList.querySelectorAll('li').forEach((tab) => {
                        tab.classList.remove('active');
                    });

                    // Add the active class to the clicked tab
                    clickedTab.classList.add('active');

                    // Hide all tab content
                    tabContent.forEach((content) => {
                        content.style.display = 'none';
                    });

                    // Show the tab content for the clicked tab
                    document.getElementById(clickedTab.dataset.tab).style.display = 'block';
                }
            });
        </script>
 